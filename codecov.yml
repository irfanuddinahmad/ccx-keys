coverage:
  status:
    project:
      default:
        target: auto
        # One-time coverage-measurement shift from this migration; see PR description.
        threshold: 5%
    patch:
      default:
        target: 90%

comment: false
